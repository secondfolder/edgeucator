node_modules

# Output
.output
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# Worker secrets for `wrangler dev`. NOTE: .env* above does NOT cover these.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Vitest
/test-results

# Playwright
/playwright-report
/blob-report
/playwright/.cache

# Local dev database (SQLite). Migrations in drizzle/ ARE committed.
local.db
local.db-shm
local.db-wal

# Playwright's database. Rebuilt from drizzle/ at the start of every e2e run.
e2e.db
e2e.db-shm
e2e.db-wal

# Encrypted attachments written by `npm run dev` (R2 stands in for this in production)
local-media/

# Miscellaneous
.local