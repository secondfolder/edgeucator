# Local development configuration. `npm run dev` needs NOTHING from Cloudflare —
# it runs against a plain SQLite file via libsql, and reads these from .env.
#
#   cp .env.example .env && npm run auth:secret

# The local dev database. Also used by drizzle-kit (generate/migrate/studio).
DATABASE_URL="file:./local.db"

# Signing/encryption key for Better Auth sessions. Generate with:
#   npm run auth:secret
# Better Auth silently falls back to a hard-coded default outside
# NODE_ENV=production, so hooks.server.ts throws if this is unset.
# In production this comes from `wrangler secret put BETTER_AUTH_SECRET` instead.
BETTER_AUTH_SECRET=""
